{
  "name": "@deque/root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "app": "pnpm --filter deque --",
    "app:build": "pnpm --filter deque -- tauri build",
    "app:dev": "pnpm --filter deque -- dev",
    "app:prisma": "cd app/src-tauri && cargo prisma --",
    "trunk": "trunk"
  },
  "engines": {
    "pnpm": ">=6.0.0",
    "npm": "pnpm",
    "yarn": "pnpm",
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "@cspell/dict-rust": "^3.0.0",
    "@cspell/dict-typescript": "^3.0.1",
    "@trivago/prettier-plugin-sort-imports": "^3.4.0",
    "@trunkio/launcher": "^1.2.2",
    "cspell": "^6.12.0",
    "prettier": "^2.7.1"
  }
}
